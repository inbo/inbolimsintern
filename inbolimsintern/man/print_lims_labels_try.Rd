% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_print_labels.R
\name{print_lims_labels_try}
\alias{print_lims_labels_try}
\title{Master function to handle API previews or direct network printing.}
\usage{
print_lims_labels_try(
  dataset,
  printer_config,
  format_lines,
  mode = c("api", "real"),
  abort_real_show_payload = FALSE,
  format = c("png", "pdf"),
  output_path = "C:/Labels/label_output.pdf",
  batch_mode = FALSE,
  batch_size = 8
)
}
\arguments{
\item{dataset}{Dataframe. The sample data to print.}

\item{printer_config}{List. Hardware settings (DPI, DARKNESS, PRINTER_PORT, etc.).}

\item{format_lines}{Dataframe. Label design definitions from T_LABEL_FORMAT_LINE.}

\item{mode}{Character. "api" for Labelary preview, "real" for physical network printing.}

\item{abort_real_show_payload}{Logical. If TRUE, stops before printing and prints ZPL to console.}

\item{format}{Character. "png" (single image) or "pdf" (multi-page batch) for API previews.}

\item{output_path}{Character. Local path to save the generated preview PDF.}

\item{batch_mode}{Logical. If TRUE, sends labels in chunks (stable). If FALSE, sends one-by-one (slower).}

\item{batch_size}{Integer. Number of labels per transmission file when batch_mode is TRUE.}
}
\value{
Invisibly returns the list of generated ZPL strings.
}
\description{
Master function to handle API previews or direct network printing.
}
\examples{
\dontrun{
# Setup data
samples <- data.frame(
  SAMPLE_ID = c("HEL001", "HEL001", "HEL002"), 
  TEXT_ID = c("24-001", "24-001", "24-002"), 
  PROJECT = "V-24G059"
)

# 1. Preview Batch as PDF
print_lims_labels(samples, p_cfg, f_lns, mode = "api", format = "pdf")

# 2. Real Print in batches of 10 for stability
print_lims_labels(samples, p_cfg, f_lns, mode = "real", batch_mode = TRUE, batch_size = 10)
}
}
