## Wijze van werken

De template files die hier staan moeten worden geplaatst onder de REPORTS_DIR van Lims
Deze folder kan je vinden in de DB: 

```
SELECT VALUE FROM CONFIG_SYS WHERE ENTRY_CODE = 'REPORTS_DIR'
```

